<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shopeehijau"
        tools:targetApi="31">

        <activity
            android:name=".activities.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Shopeehijau.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.auth.RegisterActivity" android:theme="@style/Theme.Shopeehijau.NoActionBar" />

        <activity android:name=".activities.buyer.MainActivity" android:label="Produk Kami"/>
        <activity android:name=".activities.buyer.ProductDetailActivity" android:label="Detail Produk"/>
        <activity android:name=".activities.buyer.CartActivity" android:label="Keranjang Saya"/>
        <activity android:name=".activities.buyer.CheckoutActivity" android:label="Checkout"/>
        <activity android:name=".activities.buyer.PaymentActivity" android:label="Pembayaran"/>
        <activity android:name=".activities.buyer.ProfileActivity" android:label="Profil Saya"/>

        <activity android:name=".activities.admin.AdminDashboardActivity" android:label="Dasbor Admin"/>
        <activity android:name=".activities.admin.AddProductActivity" android:label="Tambah Produk"/>
        <activity android:name=".activities.admin.EditProductActivity" android:label="Edit Produk"/>

    </application>
</manifest>